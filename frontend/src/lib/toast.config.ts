import { ToasterProps } from 'sonner'


export const Config = {
  position: 'top-center', duration: 5000, closeButton: true, richColors: true, style: {
    backgroundColor: 'var(--foreground)'
  }
} as ToasterProps;